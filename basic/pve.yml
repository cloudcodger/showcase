---
all:
  hosts:
    local_pve_datacenter:
      ansible_connection: local
      ansible_host: 127.0.0.1
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

proxmox_hosts:
  hosts:
    gismo:
      ansible_host: 192.168.6.230
